import { Routes } from '@angular/router';
import { PlusOneToArrayComponent } from './plus-one-to-array/plus-one-to-array.component';

export const routes: Routes = [
    {
        path:'',
        component:PlusOneToArrayComponent
    }
];
